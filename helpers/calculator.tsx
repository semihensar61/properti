// roman num calculator functon takes number and returns roman number as string

import { MutableRefObject, RefObject } from "react"

export const romanNumCalculator = (x: RefObject<HTMLDivElement>) : string => {
    return "Cs"
}